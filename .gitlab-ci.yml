default:
    image: registry.gitlab.com/pithos/content-management/jekyll-base

pages:
  script:
    - gem install bundler
    - bundle install
    - bundle exec jekyll build -d public
  artifacts:
    paths:
      - public
